import { SystemRequirements } from './system-requirements';

describe('SystemRequirements', () => {
  it('should create an instance', () => {
    expect(new SystemRequirements()).toBeTruthy();
  });
});
