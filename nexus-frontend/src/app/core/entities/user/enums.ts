export enum RoleType {
    ADMIN = "ADMIN",
    DEVELOPER = "DEVELOPER",
    PLAYER = "PLAYER",
}

export enum StatusFriendRequest {
    PENDING = "pending",
    ACCEPTED = "accepted",
    REJECTED = "rejected",
}

export enum NotificationType {
    PURCHASE = "purchase",
    REVIEW = "review",
    DISCOUNT = "discount",
}
